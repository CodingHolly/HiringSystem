<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.holly.back_end.mapper.SubmitResumeMapper">
    <insert id="insert">
        insert into submit_resume(id, user_phone, hr_phone, position_id, comment, submit_time, company_name,
                                  position_name)
        values (#{id}, #{userPhone}, #{hrPhone}, #{positionId}, #{comment}, #{submitTime}, #{companyName},
                #{positionName})
    </insert>
    <select id="getByUserPhoneAndPositionId" resultType="com.holly.back_end.entity.SubmitResume">
        select *
        from submit_resume
        where user_phone = #{userPhone}
          and position_id = #{positionId}
    </select>
    <select id="listAll" resultType="com.holly.back_end.entity.SubmitResume">
        select *
        from submit_resume
    </select>
</mapper>